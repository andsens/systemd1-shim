<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-Bus Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<!--
  Drop-in for /etc/dbus-1/system.d/ (loaded by the stock system.conf's own
  <includedir>). The stock default policy denies own/method_calls except
  for specific holes it punches for org.freedesktop.DBus itself - this
  punches the same kind of hole for systemd1-shim.
-->
<busconfig>
  <policy context="default">
    <allow own="org.freedesktop.systemd1"/>
    <allow send_destination="org.freedesktop.systemd1"/>
    <allow receive_sender="org.freedesktop.systemd1"/>
  </policy>
</busconfig>
